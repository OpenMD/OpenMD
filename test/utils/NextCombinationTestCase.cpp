#include "utils/NextCombinationTestCase.hpp"
#include <iostream>
#include <algorithm>
#include "utils/next_combination.hpp"
// Registers the fixture into the 'registry'
CPPUNIT_TEST_SUITE_REGISTRATION( NextCombinationTestCase);

void NextCombinationTestCase::testNextCombination() {
    std::vector<double> dv;
    std::vector<std::vector<double>::iterator> ic;

    while (next_combination(ic, dv.begin(), dv.end()) {

        
    }
}